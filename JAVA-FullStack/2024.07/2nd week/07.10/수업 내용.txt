※ 시간의 순서에 따라 기술되었습니다.

1교시
09:50
    /* 다중 행 서브 쿼리 (여러 줄 한 칸)
    - 서브 쿼리에서 반환되는 결과가 하나 이상의 행일 때 사용
    - 서브 쿼리의 SELECT절 안의 컬럼은 한 개여야만 한다.
    - 다중 행 연산자(IN, ANY, ALL)를 사용
    */

2교시
10:35
    * ALL 연산자
    (대대) 컬럼명 > ALL(여러행값)
    (소소) 컬럼명 < ALL(여러행값)

11:00
    - ANY 연산자
    (대소) 컬럼명 > ANY(여러행값)
    (소대) 컬럼명 < ANY(여러행값)

3교시
11:50
    INSERT ALL INTO 명령문 : 여러 개의 테이블에 여러 행(줄)의 데이터를 삽입할때 사용하는 명령문
    형식 1:
    INSERT ALL
    INTO 테이블명1 (컬1, 컬2, ...) VALUES (값1, 값2, ...)
    INTO 테이블명2 (컬1, 컬2, ...) VALUES (값1, 값2, ...)
    ...
    SELECT 서브쿼리명령문;

    형식 2:
    INSERT ALL
    INTO 테이블명1 VALUES (값1, 값2, ...)
    INTO 테이블명2 VALUES (값1, 값2, ...)
    ...
    SELECT 서브쿼리명령문;

    형식 3:
    INSERT ALL
    INTO 테이블명1
    INTO 테이블명2
    VALUES (값1, 값2, ...)
    ...
    SELECT 서브쿼리명령문;

5교시
14:30
    ** 다중컬럼 서브쿼리 :
    반드시 서브쿼리와 비교할 컬럼도 다중컬럼이면서 소괄호로 묶어주어야만 한다.
    (=, IN 연산자만 가능)

14:55
    테이블에 별칭을 줄 경우는 AS는 반드시 생략해야 한다.
    예) 테이블명 별칭명

7교시
16:50
    조인(JOIN) : 두 개 이상의 테이블을 결합하여 데이터를 추출하는 기법

    조인의 유형
    내부조인(INNER JOIN) : ( = 교집합)
    - 두 테이블에 공통으로 존재하는 컬럼을 이용하여 합치는 방식
    1. 동등조인(Equi Join) : 공통으로 전제하는 컬럼 값이 있는 경우의 데이터를 추출하는 조인 방식
    (INNER JOIN, ANSI INNER JOIN)
    2. 교차 조인(CROSS JOIN) : 조인 조건이 없는 조인 방식
    (CATASIAN PRODUCT, ANSI CROSS JOIN)
    총 컬럼갯수 (디그리 = 차수) : A 테이블 컬럼수 + B 테이블 컬럼수
    총 행수 (카디널리티) : A 테이블 행수 * B 테이블 행수

    외부조인(OUTER JOIN) : ( = A 또는 B 또는 합집합)
    - 특정 테이블의 모든 데이터를 기준으로 다른 테이블의 정보를 추출(다른 테이블에 값이 없어도 출력됨.)
    1. 왼쪽 외부 조인(LEFT OUTER JOIN) = A : 왼쪽 테이블의 모든 데이터와 오른쪽 테이블의 동일 데이터를 추출
    (LEFT OUTER JOIN, ANSI LEFT OUTER JOIN)
    2. 오른쪽 외부 조인 (RIGHT OUTER JOIN) = B : 오른쪽 테이블의 모든 데이터와 왼쪽 테이블의 동일 데이터를 추출
    (RIGHT OUTER JOIN, ANSI RIGHT OUTER JOIN)
    3. 완전 외부 조인(FULL OUTER JOIN) = 합집합: 양쪽의 모든 데이터를 추출
    (ANSI FULL OUTER JOIN)

    * Equi Join (동등조인, 가장 많이 사용)
    - WHERE 절에서 조인 조건을 기술
    - ANSI조인인 경우는 ON절에서 조인조건을 기술함.
    - 권장사항: 조인 조건 개수 = 연결 테이블 수 - 1
    - 명확성을 위해 컬럼명 앞에 테이블명 또는 테이블 별칭을 설정
    - 테이블의 별칭을 기술하는 경우는 공백을 이용하며, AS 키워드를 절대 사용하면 안됨.