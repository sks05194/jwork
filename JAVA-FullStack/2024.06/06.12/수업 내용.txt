※시간의 순서에 따라 기술되었으며 일부 자세한 생략이 되어있습니다.

1교시
09:30	복습


09:40	조건문 : switch~case문
	- 사용방법 : (생략)
	- 조건값의 조건 : int 이하 정수값, char, 문자열(String)


10:05	if, if~else, if~else if~else에서 명령문이 한 줄일 경우 중괄호 생략 가능.
	조건과 명령문을 한 줄에 기입하는 것이 가능하다.
	if문안에 if문을 중첩해서 기입할 수 있다.


2교시
10:40	삼항연산자
	- (조건식 또는 조건값) ? (참일 경우 실행되는 명령문) : (거짓일 경우 실행되는 명령문)


10:50	자동정렬 단축키 = Ctrl + Shift + F


11:00	문자열은 0개 이상의 문자를 가진다.
	문자열에 문자가 없으면 빈 문자열이라고 한다.
	문자는 반드시 하나의 문자를 가진다.

	null != ""(빈 문자열)

	초기화되지 않은 변수를 else나 default로 초기화시키지 않고 사용할 경우 에러가 날 수 있다.



3교시
11:45	break문이 없을 경우 아래의 case문의 실행문까지 실행된다.
	break는 분기 처리 예약어이다.

11:50	break를 감싸고 있는 가장 가까운 switch문의 종료 중괄호 밖으로 빠져나온다
		
	switch문도 if문처럼 중첩이 가능하다.

12:15	반복구조
	- 반복문: 어떠한 연산을 여러번 실행해야 하는 경우 사용, 연산의 흐름을 제어하는 제어문 중의 하나.
	(for문, while문, do~while문)

	for문 선언문 형식 : for(초기값 설정식(반복 시작값 설정); 조건식(반복을 종료하기 위해 설정); 증감식(후위연산자 사용 권장)){ /* Do Something...  */ }
	초기 설정 -> 조건식 -> 실행문 -> 증감식 -> 조건식 -> 반복 또는 종료

	for문 내의 초기값 설정식에서 선언된 변수는 for문을 빠져나가면 메모리에서 삭제된다.(변수의 생명주기)



4교시






